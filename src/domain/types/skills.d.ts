/**
 * SkillProps is an interface that defines the properties of a skill card
 * component.
 *
 * @property {string} label - The label of the skill.
 * @property {string} imageUrl - The image URL of the skill.
 * @property {boolean} favorite - The favorite status of the skill.
 */
declare interface SkillProps {
  label: string;
  imageUrl: string;
  favorite: boolean;
}
